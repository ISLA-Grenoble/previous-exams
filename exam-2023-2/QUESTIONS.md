# Exam 2023-2 — Questions

All students should do Part 1. Then choose two parts out of the remaining three (Parts 2, 3, 4).

---

## Part 1: Multiple choice questions (8 points)

Each question has exactly one correct answer.

**Q1 — Robustness to outliers (1 point)**
We consider a classification problem on linearly separable data. The dataset had an outlier — a point very far from the other datapoints. We trained LDA, logistic regression, and 1-nearest-neighbour classifiers on this dataset and tested on a test set from the same distribution but without any outlier. After that, we removed the outlier and retrained.

After retraining, which classifier will **not change** its decision boundary around the test points?
- (A) Logistic regression.
- (B) 1-nearest-neighbors classifier.
- (C) LDA.
- (D) None of them.

**Q2 — Bias-variance decomposition (1 point)**
Consider a regression model where data (x, y) is generated by x ∈ R uniformly sampled from [0, 1] and y = x + ε, where ε is random noise with mean 0 and variance 1. Model A is a trained quadratic function g_A(x, β) = β₀ + β₁x + β₂x² and model B is a constant function g_B(x) = 1/2.

Compared to model B, model A has:
- (A) Higher bias, higher variance.
- (B) Lower bias, higher variance.
- (C) Higher bias, lower variance.
- (D) Lower bias, lower variance.

**Q3 — Linear regression with L2-regularization (1 point)**
Assume linear regression with mean-squared loss and L2-regularization on four one-dimensional data points. The model is f(x) = ax + b and the optimization problem is a*, b* = argmin Σᵢ₌₁⁴ (yᵢ − f(xᵢ))² + λa². Data points: {(−2, 1), (−1, 3), (0, 2), (3, 4)}.

What is the optimal value for the bias b*?
- (A) Depends on the value of λ.
- (B) 3
- (C) 2.5
- (D) None of the above answers.

**Q4 — PCA (1 point)**
Which of the following transformations to a data matrix X will affect the principal components obtained through PCA?
- (A) Adding a constant value to all elements of X.
- (B) Multiplying one of the features of X by a constant.
- (C) Adding an extra feature to X (an extra column) that is constant across all data points.
- (D) None of the above answers.

**Q5 — PCA — left singular vectors (1 point)**
In PCA, the left singular vectors U of a data matrix X ∈ R^{N×p} are used to create X' = UᵀX. Which property always holds for X'?
- (A) X' is a square matrix.
- (B) The mean of any row X'ᵢ is 0.
- (C) X' has only positive values.
- (D) For any two rows i, j with i ≠ j from X', the dot product between the rows X'ᵢ and X'ⱼ is 0.

**Q6 — Ridge regularization (1 point)**
Assume N training samples (x₁, y₁), …, (xN, yN) where xᵢ ∈ Rᵖ and yᵢ ∈ R. For λ ≥ 0, consider:

L_λ(β) = (1/N) Σᵢ₌₁ᴺ (yᵢ − xᵢᵀβ)² + λ‖β‖₂

and let C_λ = min_{β} L_λ(β). Which statement is true?
- (A) C_λ is a non-increasing function of λ.
- (B) For λ = 0, the loss L₀ is non-convex and might have several minimizers.
- (C) C_λ is a non-decreasing function of λ.
- (D) None of the above statements are true.

**Q7 — Linear regression irreducible error (1 point)**
In linear regression, we model p(y | x) ~ N(βᵀx + β₀, σ²). The irreducible error in this model is:
- (A) σ²
- (B) E[y | x]
- (C) E[(y − E[y | x])² | x]
- (D) None of the above.

**Q8 — Classifier boundary (1 point)**
Which of these classifiers could have generated a highly non-linear, irregular decision boundary (as shown in the figure in the exam PDF)?
- (A) Logistic regression
- (B) 1-NN
- (C) Quadratic discriminant analysis
- (D) None of the above.

---

## Part 2: Weighted linear regression (6 points)

Suppose we have a regression dataset with N pairs (xᵢ, yᵢ) where xᵢ ∈ Rᵖ and yᵢ ∈ R. We wish to fit a linear model f(xᵢ) = βᵀx̃ᵢ where β has entries β₀, β₁, …, βₚ and x̃ᵢᵀ = [1 xᵢᵀ].

We minimize the following cost function:

L(β) = (1/2) Σᵢ₌₁ᴺ wᵢ (yᵢ − βᵀx̃ᵢ)²

where wᵢ > 0 are known real-valued scalars.

**(a)** Calculate the gradient of the loss function and make it equal to zero. Write an expression in matrix-vector form similar to the expressions for least-squares given in the lectures. **(2.5 points)**

**(b)** Discuss the conditions under which the solution β* is unique. **(1.5 points)**

**(c)** Assuming those conditions hold, write down the expression for the unique solution. **(1 point)**

**(d)** Interpret the role of the weights wᵢ in the results of the regression. Explain how adding them to the usual linear regression model can be useful for certain situations. **(1 point)**

---

## Part 3: Principal component analysis (6 points)

We consider the dataset `cars04`, which describes several properties of different car models in the market in 2004. Each car is described by 11 features:

| Variable     | Meaning |
|--------------|---------|
| Retail       | Builder recommended price (US$) |
| Dealer       | Seller price (US$) |
| Engine       | Motor capacity (liters) |
| Cylinders    | Number of cylinders in the motor |
| Horsepower   | Engine power |
| CityMPG      | Consumption in city (miles per gallon) |
| HighwayMPG   | Consumption on roadway (miles per gallon) |
| Weight       | Weight (pounds) |
| Wheelbase    | Distance between front and rear wheels (inches) |
| Length       | Length (inches) |
| Width        | Width (inches) |

```r
cars04 <- read.csv("cars-fixed04.csv")[,8:18]
cars04.pca <- prcomp(cars04, scale=TRUE)
summary(cars04.pca)
```

**(a)** What is the effect of the argument `scale=TRUE` in the result of the PCA? **(1.5 points)**

**(b)** Are the first two principal components enough to summarize most of the information (variance) of the dataset? Justify in terms of the proportion of the total variance they represent. **(1.5 points)**

**(c)** What would be a good interpretation for the first 2 principal components in terms of the initial features of the dataset? **(1 point)**

**(d)** Interpret each quadrant of the PCA individual-space figure. **(1 point)**

**(e)** Can you describe which kind of car the following are: Audi RS 6, Ford Expedition 4.6 XLT, and Nissan Sentra 1.8? **(1 point)**

---

## Part 4: Spectral community detection (6 points)

These questions relate to the article "Modularity and community structure in networks" by Mark Newman.

**(a)** Give two examples of applications of community detection in networks in real life. **(1 point)**

**(b)** Give the definition of modularity and explain why it is a good target quantity when investigating community structures in networks. **(1.5 points)**

**(c)** What is the interpretation in terms of community detection when the modularity matrix of a network has no positive eigenvalues? **(1 point)**

**(d)** What information does the magnitude of each coordinate of the leading eigenvector of the modularity matrix convey regarding the split of a network into two communities? **(1.5 points)**

**(e)** What is the procedure proposed by the author for splitting a network into more than two communities using his spectral algorithm? Is it capable of proposing a split into three communities? Why? **(1 point)**
